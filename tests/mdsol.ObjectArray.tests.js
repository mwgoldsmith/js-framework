/*global module,test,ok*/
(function (mdsol) {
    module('mdsol.ObjectArray');

    test('pop', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.pop === 'function', 'exists');

        // TODO: Create tests
    });

    test('push', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.push === 'function', 'exists');

        // TODO: Create tests
    });

    test('reverse', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.reverse === 'function', 'exists');

        // TODO: Create tests
    });

    test('shift', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.shift === 'function', 'exists');

        // TODO: Create tests
    });

    test('sort', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.sort === 'function', 'exists');

        // TODO: Create tests
    });

    test('splice', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.splice === 'function', 'exists');

        // TODO: Create tests
    });

    test('unshift', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.unshift === 'function', 'exists');

        // TODO: Create tests
    });

    test('slice', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.slice === 'function', 'exists');

        // TODO: Create tests
    });

    test('contains', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.contains === 'function', 'exists');

        // TODO: Create tests
    });

    test('filter', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.filter === 'function', 'exists');

        // TODO: Create tests
    });

    test('get', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.get === 'function', 'exists');

        // TODO: Create tests
    });

    test('getAll', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.getAll === 'function', 'exists');

        // TODO: Create tests
    });

    test('getNot', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.getNot === 'function', 'exists');

        // TODO: Create tests
    });

    test('getUnique', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.getUnique === 'function', 'exists');

        // TODO: Create tests
    });

    test('indexOf', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.indexOf === 'function', 'exists');

        // TODO: Create tests
    });

    test('lastIndexOf', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.lastIndexOf === 'function', 'exists');

        // TODO: Create tests
    });

    test('move', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.move === 'function', 'exists');

        // TODO: Create tests
    });

    test('pluck', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.pluck === 'function', 'exists');

        // TODO: Create tests
    });

    test('size', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.size === 'function', 'exists');

        // TODO: Create tests
    });

    test('unique', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.unique === 'function', 'exists');

        // TODO: Create tests
    });

    test('where', function () {
        var obj = mdsol.ObjectArray();
        
        ok(typeof obj.where === 'function', 'exists');

        // TODO: Create tests
    });

} (window.mdsol));