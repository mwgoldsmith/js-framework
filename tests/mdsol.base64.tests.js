/*global module,test,ok*/
(function (mdsol) {
    module('mdsol.base64');

    test('base64Decode', function () {
        ok(typeof mdsol.base64Decode === 'function', 'exists');

        // TODO: Create tests
    });

    test('base64Encode', function () {
        ok(typeof mdsol.base64Encode === 'function', 'exists');

        // TODO: Create tests
    });

} (window.mdsol));