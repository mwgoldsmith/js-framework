/*global module,test,ok*/
(function (mdsol) {
    module('mdsol.cookies');
    
    test('getCookie', function () {
        ok(typeof mdsol.getCookie === 'function', 'exists');

        // TODO: Create tests
    });

    test('setCookie', function () {
        ok(typeof mdsol.setCookie === 'function', 'exists');

        // TODO: Create tests
    });

    test('deleteCookie', function () {
        ok(typeof mdsol.deleteCookie === 'function', 'exists');

        // TODO: Create tests
    });

} (window.mdsol));