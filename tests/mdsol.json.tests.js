/*global module,test,ok*/
(function (mdsol) {
    module('mdsol.json');

    test('parseJson', function () {
        ok(typeof mdsol.parseJson === 'function', 'exists');

        // TODO: Create tests
    });

    test('toJson', function () {
        ok(typeof mdsol.toJson === 'function', 'exists');

        // TODO: Create tests
    });

} (window.mdsol));