/*global module,test,ok*/
(function (mdsol) {
    module('mdsol.strings');

    test('alphaNumeric', function () {
        ok(typeof mdsol.alphaNumeric === 'function', 'exists');

        // TODO: Create tests
    });
    
    test('trim', function () {
        ok(typeof mdsol.trim === 'function', 'exists');

        // TODO: Create tests
    });

} (window.mdsol));